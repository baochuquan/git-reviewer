{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "rdbg",
            "name": "Debug",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "command": "bundle exec ruby",
            "script": "${workspaceFolder}/exe/git-reviewer",
            "args": ["--target=master", "--verbose"]
        }
    ]
}